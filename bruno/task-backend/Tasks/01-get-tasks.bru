meta {
  name: Get Tasks
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/tasks/getTasks
  body: none
  auth: bearer
}

params:query {
  page: 1
  limit: 20
  status: {{taskStatus}}
  keyword: {{taskKeyword}}
}

auth:bearer {
  token: {{accessToken}}
}
