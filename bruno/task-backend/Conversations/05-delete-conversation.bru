meta {
  name: Delete Conversation
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/api/conversations/deleteConversation/{{conversationId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}
