meta {
  name: Add Participant
  type: http
  seq: 6
}

post {
  url: {{baseUrl}}/api/conversations/addParticipant/{{conversationId}}
  body: json
  auth: bearer
}

headers {
  Content-Type: application/json
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "userId": "{{otherUserId}}"
  }
}
