meta {
  name: Test Notification To Me
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/api/devices/test-notification-to-me
  body: json
  auth: bearer
}

headers {
  Content-Type: application/json
  Accept-Language: {{language}}
  X-Request-Id: {{requestId}}
  X-App-Version-Code: {{appVersionCode}}
  X-App-Version-Name: {{appVersionName}}
  X-Platform: {{platform}}
  X-OS-Version: {{osVersion}}
  X-Device-Model: {{deviceModel}}
  X-Device-Id: {{deviceId}}
  User-Agent: {{userAgent}}
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "title": "Test Notification",
    "body": "This is a test push notification.",
    "data": {
      "type": "test"
    }
  }
}

docs {
  # Test Notification To Me

  ## Mô tả

  Gửi FCM push notification thử nghiệm đến tất cả device token đã đăng ký của người dùng đang đăng nhập.

  Endpoint này dùng `Authorization: Bearer {{accessToken}}` để xác định user, sau đó backend tự query `DeviceToken` theo `userId`. Bruno không cần truyền `deviceFcmToken`.

  **Domain:** Devices — quản lý push notification target cho từng user.

  **URL:** `POST /api/devices/test-notification-to-me`

  ---

  ## cURL

  ```bash
  curl -X POST "{{baseUrl}}/api/devices/test-notification-to-me" \
    -H "Content-Type: application/json" \
    -H "Authorization: Bearer {{accessToken}}" \
    -H "Accept-Language: vi" \
    -H "X-Request-Id: req-device-003" \
    -H "X-App-Version-Code: 10" \
    -H "X-App-Version-Name: 1.0.0" \
    -H "X-Platform: android" \
    -H "X-OS-Version: 14" \
    -H "X-Device-Model: Pixel 7" \
    -H "X-Device-Id: device-xyz" \
    -H "User-Agent: TaskApp/1.0.0" \
    -d '{"title":"Test","body":"Hello from server","data":{"type":"test"}}'
  ```

  ---

  ## Headers

  | Header | Bắt buộc | Mô tả |
  |---|---|---|
  | `Content-Type` | ✅ | Định dạng body — phải là `application/json` |
  | `Authorization` | ✅ | Bearer token xác thực người dùng |
  | `Accept-Language` | ✅ | Ngôn ngữ phản hồi lỗi (`vi`, `en`) |
  | `X-Request-Id` | ✅ | ID định danh request, dùng để trace log |
  | `X-App-Version-Code` | ✅ | Version code của app |
  | `X-App-Version-Name` | ✅ | Version name của app |
  | `X-Platform` | ✅ | Nền tảng client (`android`, `ios`, `web`) |
  | `X-OS-Version` | ✅ | Phiên bản OS của thiết bị |
  | `X-Device-Model` | ✅ | Model thiết bị |
  | `X-Device-Id` | ✅ | ID duy nhất của thiết bị |
  | `User-Agent` | ✅ | Thông tin client app |

  ---

  ## Request Body

  | Field | Kiểu | Bắt buộc | Mô tả |
  |---|---|---|---|
  | `title` | `string` | ❌ | Tiêu đề notification; mặc định `"Test Notification"` |
  | `body` | `string` | ❌ | Nội dung notification; mặc định `"This is a test push notification."` |
  | `data` | `object` | ❌ | Custom data payload key-value strings; mặc định `{}` |

  ---

  ## Response

  ### ✅ Success — 200 OK

  ```json
  {
    "success": true,
    "message": "Test notification sent successfully.",
    "data": {
      "sentCount": 1,
      "failedCount": 0,
      "removedInvalidTokenCount": 0,
      "messageIds": [
        "projects/your-project/messages/0:1234567890"
      ]
    }
  }
  ```

  | Field | Kiểu | Mô tả |
  |---|---|---|
  | `success` | `boolean` | `true` nếu FCM chấp nhận message |
  | `message` | `string` | Thông báo xác nhận |
  | `data.sentCount` | `number` | Số device token gửi thành công |
  | `data.failedCount` | `number` | Số device token gửi thất bại |
  | `data.removedInvalidTokenCount` | `number` | Số token invalid đã bị backend dọn khỏi DB |
  | `data.messageIds` | `string[]` | Message ID trả về từ FCM — dùng để trace trên Firebase Console |

  ### ❌ Error Responses

  | HTTP Status | Error Code | Mô tả |
  |---|---|---|
  | `401` | `AUTH_ACCESS_TOKEN_REQUIRED` | Thiếu access token |
  | `401` | `AUTH_INVALID_TOKEN` | Access token không hợp lệ hoặc hết hạn |
  | `404` | `DEVICE_TOKENS_NOT_FOUND` | User chưa register FCM token nào |
  | `500` | `DEVICE_SEND_NOTIFICATION_FAILED` | Lỗi server không xác định khi gọi FCM |

  Response lỗi gửi FCM có thể kèm `data.errors[]` để debug Firebase Admin SDK:

  ```json
  {
    "success": false,
    "code": "DEVICE_SEND_NOTIFICATION_FAILED",
    "message": "Gửi thông báo thử nghiệm thất bại.",
    "requestId": "req-device-003",
    "data": {
      "failedCount": 3,
      "removedInvalidTokenCount": 0,
      "errors": [
        {
          "code": "messaging/mismatched-credential",
          "message": "Sender ID mismatch"
        }
      ]
    }
  }
  ```

  ---

  ## Notes

  - Trước khi gọi endpoint này, user phải gọi `POST /api/devices/register-token` ít nhất một lần từ app Android thật.
  - FCM trả về `200` ngay cả khi device offline; notification sẽ được deliver khi device online lại.
  - Nếu FCM báo token invalid/not registered, backend sẽ tự xóa token đó khỏi DB.
  - `data` values phải là string — FCM không hỗ trợ nested objects trong data payload.
}
