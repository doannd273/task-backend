meta {
  name: Unregister Device Token
  type: http
  seq: 2
}

delete {
  url: {{baseUrl}}/api/devices/unregister-token
  body: json
  auth: bearer
}

headers {
  Content-Type: application/json
  Accept-Language: {{language}}
  X-Request-Id: {{requestId}}
  X-App-Version-Code: {{appVersionCode}}
  X-App-Version-Name: {{appVersionName}}
  X-Platform: {{platform}}
  X-OS-Version: {{osVersion}}
  X-Device-Model: {{deviceModel}}
  X-Device-Id: {{deviceId}}
  User-Agent: {{userAgent}}
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "token": "{{deviceFcmToken}}"
  }
}

docs {
  # Unregister Device Token

  ## Mô tả

  Xóa FCM device token của người dùng đang đăng nhập. Endpoint chỉ xóa document khớp cả `userId` từ JWT và `token` trong body.

  **Domain:** Devices — quản lý push notification target cho từng user.

  **URL:** `DELETE /api/devices/unregister-token`

  ---

  ## cURL

  ```bash
  curl -X DELETE "{{baseUrl}}/api/devices/unregister-token" \
    -H "Content-Type: application/json" \
    -H "Authorization: Bearer {{accessToken}}" \
    -H "Accept-Language: vi" \
    -H "X-Request-Id: req-device-002" \
    -H "X-App-Version-Code: 10" \
    -H "X-App-Version-Name: 1.0.0" \
    -H "X-Platform: android" \
    -H "X-OS-Version: 14" \
    -H "X-Device-Model: Pixel 7" \
    -H "X-Device-Id: device-xyz" \
    -H "User-Agent: TaskApp/1.0.0" \
    -d '{"token":"fcm-demo-token"}'
  ```

  ---

  ## Headers

  | Header | Bắt buộc | Mô tả |
  |---|---|---|
  | `Content-Type` | ✅ | Định dạng body — phải là `application/json` |
  | `Authorization` | ✅ | Bearer token xác thực người dùng |
  | `Accept-Language` | ✅ | Ngôn ngữ phản hồi lỗi (`vi`, `en`) |
  | `X-Request-Id` | ✅ | ID định danh request, dùng để trace log |
  | `X-App-Version-Code` | ✅ | Version code của app |
  | `X-App-Version-Name` | ✅ | Version name của app |
  | `X-Platform` | ✅ | Nền tảng client (`android`, `ios`, `web`) |
  | `X-OS-Version` | ✅ | Phiên bản OS của thiết bị |
  | `X-Device-Model` | ✅ | Model thiết bị |
  | `X-Device-Id` | ✅ | ID duy nhất của thiết bị |
  | `User-Agent` | ✅ | Thông tin client app |

  ---

  ## Request Body

  | Field | Kiểu | Bắt buộc | Mô tả |
  |---|---|---|---|
  | `token` | `string` | ✅ | FCM registration token cần xóa |

  ---

  ## Response

  ### ✅ Success — 200 OK

  ```json
  {
    "success": true,
    "data": {
      "message": "Device token unregistered successfully."
    }
  }
  ```

  | Field | Kiểu | Mô tả |
  |---|---|---|
  | `success` | `boolean` | `true` nếu xóa thành công |
  | `data.message` | `string` | Thông báo xác nhận xóa token |

  ### ❌ Error Responses

  | HTTP Status | Error Code | Mô tả |
  |---|---|---|
  | `400` | `DEVICE_REGISTER_REQUIRED_FIELDS` | Thiếu `token` |
  | `401` | `AUTH_ACCESS_TOKEN_REQUIRED` | Thiếu access token |
  | `401` | `AUTH_INVALID_TOKEN` | Access token không hợp lệ hoặc hết hạn |
  | `404` | `DEVICE_UNREGISTER_NOT_FOUND` | Token không tồn tại hoặc không thuộc user này |
  | `500` | `DEVICE_UNREGISTER_FAILED` | Lỗi server không xác định |

  ---

  ## Notes

  - Server không cho xóa token của user khác vì filter luôn có `userId`.
  - Nếu gọi unregister lần hai với cùng token, response sẽ là `404 DEVICE_UNREGISTER_NOT_FOUND`.
}
