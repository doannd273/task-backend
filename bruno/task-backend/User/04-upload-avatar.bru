meta {
  name: Upload Avatar
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/api/user/uploadAvatar
  body: multipartForm
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:multipart-form {
  avatar: @file({{avatarPath}})
}
